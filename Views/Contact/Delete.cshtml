@model frontend_com_aspnet_mvc.Models.Contact;

@{
    ViewData["Title"] = "Delete Contact";
}

<h2 style="color: red;">Are you sure you want to delete this contact? This operation cannot be undone.</h2>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Name)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Name)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.PhoneNumber)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.PhoneNumber)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Active)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Active)
        </dt>
    </dl>
</div>

<form asp-action="Delete">
    <input type="submit" value="Delete" class="btn btn-danger" />
</form>
<a asp-action="Index">Cancel</a>